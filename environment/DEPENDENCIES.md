# Dependencies

The final scripts use Python standard-library modules for file handling, argument parsing, hashing, and dynamic loading. Numerical tables use NumPy and pandas. Statistical and sparse-matrix operations use SciPy. Figure rendering uses Matplotlib and Pillow with a non-interactive backend.

No network service, database, notebook server, or graphical desktop session is required for the supplied figure renderer.
